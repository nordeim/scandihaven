Session 10 shows round 5 completed and pushed (origin/main at 3061cf0) with three recommended next steps: redeploy verification, NEXT_PUBLIC_SITE_URL setup, and the queued R-DB-1/R-DB-2 P0s. Let me first verify what state the deployed sites are in and whether the round-5 fixes went live:
Live deployment check: repo E2E suite vs https://scandihaven.jesspete.shop = **46/46 PASS** — the round-5 fixes (R5-2 canonical/og:url sitewide, R5-3 empty-category 200s) are live. Health endpoints on both origins return `{status:ok,db:true}`. The three "pending redeploy" failures from session 10 are gone. Baseline local gates: lint 8/8, typecheck 8/8, tests 7/7 (commerce 90.9%/92.81% — above gate), build 2/2 per the checked-in start_server_log.txt.
A comprehensive live sweep (31 storefront routes, 8 admin routes, security headers, robots/sitemap parity, canonical/og:url, JSON-LD, typeahead contract, TTFB ~140ms vs the 800ms SLO) plus browser probes of checkout, PDP variant deep-links, mini-cart drawer, admin sign-in errors, and the newsletter form came back clean — but with three genuine findings. Let me validate each against the codebase:
**Finding 1 (P0)**: `git ls-files` shows `.env` TRACKED again — commit `316befa` ("new start server log") re-added the 51-line file with real `BETTER_AUTH_SECRET` + `CRON_SECRET` values, the exact C2/R4-1 re-exposure shape that 4afec43 untracked in round 4. The CI secret-scan pattern executed verbatim locally flags `.env:7` and `.env:31`, so the next CI run on main would go red until it is untracked. (Also investigated: `branches: ain]` in ci.yml looked like a corrupted trigger — it is NOT: Python YAML parses it as `['main']`; the bracket-eating is the same terminal display artifact session 10 documented for git output `ain 7eec00f]`.)
**Finding 2 (HIGH, FR-101/FR-104)**: the desktop header (`site-header.tsx`) and the mobile drawer (`mobile-nav.tsx`) carry **no search affordance at all** — live home HTML has zero search inputs. The typeahead API (rate-limited 60/min/IP, Zod-validated) and the `/search` results page (FR-106) exist but are unsurfaced dead ends; the route comment even says "reserved for the FR-104 expansion — search slice in the remediation backlog". Traceability marks FR-104 "Deferred (Phase 1)" — but FR-104 is M-priority ("must ship v1") and it is the biggest user-visible gap on the live site.
**Finding 3 (MEDIUM)**: `apps/admin` has **no browser coverage whatsoever** — no e2e dir, no Playwright config, no e2e script; CI's E2E step scopes `@scandihaven/web` only. PRD §12.1 lists admin flows among the E2E critical paths. **Finding 4 (docs)**: PAD §11 still marks R-SEO-1 (sitemap/robots) and R-SEO-2 (/search) as "Open — P0" though they shipped in `180cc54` and are live-verified this round.
Embedded PG 17.5 provisioned (no docker in this sandbox): zonky binaries via npm `@embedded-postgres/linux-x64@17.5.0-beta.15`, manual initdb + pg_ctl, extensions pgcrypto/pg_trgm/citext, migrate+seed green. Note: the sandbox's own `DATABASE_URL` env var must be overridden — migrate refuses non-local hosts (M6d guard working as designed).
Now let me compile the findings and write the round-6 remediation plan:
## Round 6 TDD Execution
### Slice 1 — R6-1: untrack .env (RED already captured: `git ls-files` + scan flags)
`git rm --cached .env` (working-tree copy preserved), CI-checkout simulation scans clean. Committed atomically.
### Slice 2 — R6-2: header search typeahead (FR-101/FR-104)
RED: 7 new E2E specs + 6 unit assertions, all failing. GREEN implementation:
- `lib/search-suggest.ts` — pure response shaping (2-char route minimum, 10-result cap, malformed-payload tolerance). One unit-test bug found mid-slice (wrong expected group order) — fixed the test, not the code.
- `components/search-trigger.tsx` — ARIA 1.2 combobox, 250 ms debounce, abortable fetches, ArrowUp/Down/Enter/Escape, bare submit → `/search?q=`.
- The typeahead route now serves categories via the new `commerce/catalog.searchTypeaheadCategories` (kept a separate function so the SearchProvider port's product-row contract is untouched); the journal group stays empty until the FR-703 reader route exists — advertising journal hits would link to 404s (FR-109 honesty).
- Header mounts the combobox (desktop); the mobile drawer gains a Search link (FR-102 parity).
Two real lessons captured as repo conventions: (1) the explicit `role="combobox"` OVERRIDES the implicit `searchbox` role — `getByRole("searchbox")` finds nothing (cost a RED cycle); (2) ESLint's `react-hooks/set-state-in-effect` rejects the effect-body setState pattern — dropdown visibility became DERIVED state (`focused × ready × has-options`), which is also the better React. A third environmental lesson: `NEXT_PUBLIC_SITE_URL` is inlined at build time, so local canonical/og:url spec runs need the CI value set before building (the three "failures" with the production value inlined were an env artifact, not regressions — CI sets `http://127.0.0.1:3000` at job level).
GREEN: **53/53** local full suite (46 prior + 7 new), lint 8/8, typecheck 8/8, tests 7/7.
### Slice 3 — R6-3: admin gate E2E (characterization specs — green-by-construction)
8 credential-free specs: unauth 307s preserve `?redirect=` for `/`, `/orders`, `/products`, `/customers`, and unknown paths (H2-ADMIN/E2E-5 contract); the sign-in form renders; §9.3 headers apply to `/sign-in` (H5d); wrong credentials produce a typed error and the gate keeps holding (single attempt per run — auth rate limit is 5/min/IP+email). `@playwright/test` added to the admin workspace via `pnpm add` (lockfile via pnpm only), config mirrors web's, CI gains an "E2E admin (Chromium)" step. GREEN **8/8 vs the local prod build AND 8/8 vs the live admin origin** — the live gate contract is pinned against regression for the first time.
### Slice 4 — R6-4 + docs alignment
PAD §11: R-SEO-1/R-SEO-2 rows flipped Open→Resolved with round-4 commit + round-6 live evidence; the facet-indexing row is now honestly marked blocked-by-R-SHOP-2 (no filter UI emits facet URLs yet). Traceability: FR-104 Deferred→Aligned with the new loci, FR-101 note, FR-106 spec count. Verification ledger round-6 entry appended. AGENTS.md/CLAUDE.md conventions updated (combobox role override, derived-state rule, NEXT_PUBLIC build-time inlining, the .env re-tracking lesson with a pre-commit check).
## Final verification before push
Full gate suite green: lint 8/8, typecheck 8/8, tests 7/7 (integration suites live on PG 17.5), build 2/2, Playwright web 53/53 + admin 8/8 local (admin also 8/8 vs live). `skills/` and `infrastructure/` untouched throughout. Byte-level commit check before push (round-4 lesson).
